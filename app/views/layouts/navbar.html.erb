<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Forest77</a>
    </div>
    <div class="collapse navbar-collapse" >
      <ul class="nav navbar-nav navbar-left">
        <% unless @isLogin then %>
        <% else %>
          <li><a href="<%=url_for :controller=>:project_manage, :action=>:index %>" >Projects</a></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
          <% unless @isLogin then %>
            <li><a href="<%=url_for :controller=>:home, :action=>:login %>" >Login</a></li>
          <% else %>
            <li class="dropdown" >
              <a href="#" class="dropdown-toggle" style="color:#AAA;" type="button"
                data-toggle="dropdown"><%=@user.name%><span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="<%=url_for :controller=>:user, :action=>:change_password %>">Modify Password</a></li>
                <li><a href="<%=url_for :controller=>:home, :action=>:logout %>" >Logout</a></li>
              </ul>
            </li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>